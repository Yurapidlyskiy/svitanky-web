# Testing Rules

## Integration Tests
- Focus on Server Components (Containers) and Server Actions to ensure data flows correctly between the database and the UI.

## Unit Tests
- Focus heavily on Custom Hooks (testing pure logic variations) and complex UI components.
